# requirements.txt

# --- Core GUI & Audio ---
customtkinter>=5.2.0
sounddevice>=0.4.6
numpy>=1.20.0
# Pillow wird normalerweise von customtkinter selbst installiert, falls benötigt.

# --- Speech-to-Text Engines ---
openai>=1.0.0          # Für OpenAI Whisper API
elevenlabs>=1.0.0      # Für ElevenLabs STT API
soundfile>=0.12.0      # Für Audio-Formatierung (API Input)

# --- WebSocket für externe Steuerung / Streamer.bot ---
websockets>=12.0

# --- Encryption ---
cryptography>=40.0.0

# --- Optional: Lokales Whisper ---
# Für lokales Whisper zusätzlich installieren (siehe auch Code-Kommentare):
# 1. ffmpeg (System-Installation: https://ffmpeg.org/download.html)
# 2. Das Whisper-Paket:
#    pip install -U openai-whisper
# 3. Optional für GPU-Nutzung:
#    PyTorch mit CUDA-Unterstützung (siehe https://pytorch.org/)

# --- Optional für Whisper Build (selten nötig) ---
# setuptools-rust
